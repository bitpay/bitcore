{
  "version": "8.0.0",
  "prettier": {
    "singleQuote": true
  },
  "scripts": {
    "build": "docker build -t bitcore-node .",
    "postinstall": "./node_modules/.bin/lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^2.9.1"
  }
}
