export interface Config {
  elasticHost: string;
  elasticPort: string;
  elasticVersion: string;
  elasticLogLevel: string;
  chain: string;
  network: string;
  protocol: string;
  rpcPort: string;
  rpcUser: string;
  rpcPass: string;
}
