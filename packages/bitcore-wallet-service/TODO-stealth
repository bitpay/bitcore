- define encoding... checksum?
- add output suffling

- refactor + optimize tx detection
- use prefixes for detection / broadcasting
- add incomming TXs to wallet balance
- detect incoming OLD/mined txs



bitcore(new tx with op_ret)

bitcore(get txs with op_ret prefix (xxxx))



Proto changes
- ScanPub Priv key = f(xpub0,1,2,3)
- changed encoding to base32. up to n=3 wallet are ok
- multisig leaks wallet configuration 2-3, etc.
- lexicogracical ordered for multisig





