#!/bin/sh

MODULES=' crypto-wallet-core bitrupee-wallet-service bitrupee-wallet-client bitrupee-client'

for i in $MODULES;
do
  echo "## $i"
  cd $i && npm run compile || (echo "ERROR" && exit) && cd ..
done
